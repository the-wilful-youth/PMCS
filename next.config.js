/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Uncomment if using the App Router (default in Next 13+)
  // experimental: { appDir: true },
};
export default nextConfig;